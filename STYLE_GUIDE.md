# material_design_dark_hires_v2 — 處理規格書

這份文件記錄這套游標主題目前套用的高解析度處理方式,方便日後重現或套用到其他檔案。

## 1. 設計顏色

| 用途 | 色碼 | RGB |
|---|---|---|
| 主體(navy) | `#263238` | 38, 50, 56 |
| 邊緣高光(white) | `#FFFFFF` | 255, 255, 255 |

原始 32×32 素材裡,這兩色之間的抗鋸齒漸層取「平均亮度 luminance = (R+G+B)/3」在 **48 (navy) ~ 255 (white)** 之間內插,天然中點在 **151.5**。

## 2. 風格調整:白邊變窄 / navy 主體加大

目的:讓深藍主體感覺更飽滿,白色描邊不要太搶眼。

做法:不是簡單的硬切,而是對「navy↔white 混合比例 t」做 gamma 曲線壓縮,兩端(純 navy / 純 white)不變,中間往 navy 偏:

```
lum = (R + G + B) / 3
t   = clip((lum - 48) / (255 - 48), 0, 1)      # 0=純navy, 1=純white
t'  = t ** GAMMA                                # GAMMA > 1 讓中間值往 0(navy)偏
new_RGB = navy + t' * (white - navy)
alpha 不變(完全不動,形狀/邊緣輪廓維持原樣)
```

**目前採用 `GAMMA = 1.84`**,等效於把「navy/white 分界的判定門檻」從天然中點 151.5 提高到約 190(滿刻度 255 中),也就是原本會被判成白色的中間灰階,現在大約有 68% 的範圍會被判成 navy。

這一步**只改顏色,不改 alpha/形狀**,可以套用在任何已經做好形狀處理的圖層上(向量或點陣皆可),不會重新引入鋸齒。

## 3. 尖角/形狀處理(現已套用於全部 15 個 `.cur`)

放大後尖角出現鋸齒/分岔,原因是傳統點陣縮放(Lanczos)在銳角處必然出現 ringing、且無法還原原圖 32px 取樣不足的細節。改用「次像素邊界重建」(實作見 `tools/reconstruct.py`):

1. 取原圖(32px)alpha 通道,用 marching squares(`skimage.measure.find_contours`, level=127.5)找出「透明↔實色」的次像素精確外輪廓 —— 直接讀出抗鋸齒隱含的真實邊界位置,不是先二值化再描邊。陣列先 pad 1px 再減回座標,避免形狀貼著畫布邊緣時 marching squares 在角落生成幻影輪廓。
2. 用 luminance 通道、以本文件第 2 節的門檻(level≈190)找 navy/white 內部分界的次像素輪廓,過濾掉貼著外輪廓的偽輪廓(antialiasing 邊緣本身偏亮造成的假邊,面積 >60% 外輪廓面積者捨棄)。alpha<32 的近乎全透明邊緣像素(可能殘留高亮度色值)一律視為 navy,避免污染 luminance 輪廓。
3. 若整張圖幾乎找不到 navy(38,50,56)(例如 `unavailiable.cur` 已被改成紅色描邊+透明底,不再是 navy/white 雙色),改用該圖實際的主色當底色,且不找內部白邊輪廓 —— 單色圖形只重建外輪廓。
4. 組成 SVG:主色填滿外輪廓;white(雙色圖才有)用「外輪廓 + 內輪廓」合併成一個 `fill-rule="evenodd"` 路徑畫出環狀區域,讓內部核心維持主色(不會被誤塗白)。
5. 用 `resvg` 以正確抗鋸齒渲染到 32/48/64/96/128 五個尺寸,取代原始點陣插值,熱點沿用原檔案每個尺寸既有的數值。

這個方法**保留原圖真實形狀**(不是 vtracer 那種先二值化再讓曲線擬合跑掉樣子的做法),同時邊緣是真正平滑的曲線。多數圖示的邊緣抗鋸齒過渡帶寬度從平均 1.5~2px(個別邊緣可達 40px 以上)收斂到跟 `pointer.cur` 同等的 ~1.2~1.6px。

`busy.ani`/`work.ani` 目前**不在此列**——動畫每一幀都要各自重建,對這兩個圓形/光暈為主的圖案(本來就沒有銳利尖角)投入產出比低,暫時維持原本的點陣流程。

## 4. 多尺寸內嵌

- `.cur`:內嵌 32 / 48 / 64 / 96 / 128 五個尺寸(熱點依比例縮放)。
- `.ani`:內嵌 32 / 64 兩個尺寸(避免每個動畫幀都乘 5 種尺寸造成檔案暴增)。

## 5. 檔案對照

| 檔案 | 形狀處理 | 顏色風格 |
|---|---|---|
| 全部 15 個 `.cur`(含 `unavailiable.cur`) | 次像素邊界重建(第 3 節) | 雙色圖:GAMMA=1.84(第 2 節);`unavailiable.cur`:單色紅,不套用 |
| `busy.ani` / `work.ani` | 原 v2 點陣流程(不變) | GAMMA=1.84(第 2 節) |
